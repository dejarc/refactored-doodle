version: 2.1
jobs:
  print_hello_and_fail:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run : |
          echo "hello world" && return 1
      - run:
          when: on_fail
          command: echo "Hello Error!"
workflows:
  workflow:
    jobs:
      - print_hello_and_fail
