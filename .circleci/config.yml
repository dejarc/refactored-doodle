version: 2.1
jobs:
  print_hello_and_fail:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run : echo "hello world" && return 1
      - when:
          condition: on_fail
          steps:
            - run: echo "failure caught"
workflows:
  workflow:
    jobs:
      - print_hello_and_fail
